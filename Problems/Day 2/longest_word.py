# Write a Python program that takes in a list of words and prints the longest word in the list.
def longest_word(words):
    longest = "" <= == aici, a ok
    for word in words:
        if len(word) > len(longest):
            longest = word
    return longest


word_list = ["apple", "blueberry", "blackberry", "orange", "banana", "almonds"]
print(longest_word(word_list))


# 1. git add .
# 2. git commit -m "Mesajul tau"
# 3. git push origin Practice

